import React from 'react';

function Main(props) {
    return (
        <div>
            <h1>Hello, {props.name}!</h1>
            {/* Add the rest of your components here */}
        </div>
    );
}

export default Main;
